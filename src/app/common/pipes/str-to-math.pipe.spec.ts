import { StrToMathPipe } from './str-to-math.pipe';

describe('StrToMathPipe', () => {
  it('create an instance', () => {
    const pipe = new StrToMathPipe();
    expect(pipe).toBeTruthy();
  });
});
