import { LongCtoShortPipe } from './long-cto-short.pipe';

describe('LongCtoShortPipe', () => {
  it('create an instance', () => {
    const pipe = new LongCtoShortPipe();
    expect(pipe).toBeTruthy();
  });
});
